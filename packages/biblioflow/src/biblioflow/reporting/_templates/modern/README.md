# biblioflow modern report template

The initial reporting implementation renders QMD from the core Python template
in `biblioflow.reporting.renderer`. This directory is packaged as the home for
future Quarto partials, Typst themes, and static assets so downstream packages
can use the same core assets instead of duplicating report templates.
